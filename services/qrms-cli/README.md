# QRMS CLI

The QRMS CLI is located in `services/qrms/src/bin/qrms-cli.rs`.

This directory is reserved for future CLI tooling and utilities.

## Usage

```bash
cd services/qrms
cargo run --release --bin qrms-cli
```
